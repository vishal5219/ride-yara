import React from 'react';
import { Box, Typography } from '@mui/material';

function BookingListPlaceholder() {
  return (
    <Box sx={{ mt: 2 }}>
      <Typography variant="body2">Booking list will be rendered here</Typography>
    </Box>
  );
}

export default BookingListPlaceholder; 